# SIG-UTCUTS Chile — Environment Variables
# Copy this file to .env and adjust values as needed.
# NEVER commit .env with real secrets.

# PostgreSQL / PostGIS
POSTGRES_USER=sigutcuts
POSTGRES_PASSWORD=sigutcuts_dev_2024
POSTGRES_DB=sigutcuts
DATABASE_URL=postgresql://sigutcuts:sigutcuts_dev_2024@db:5432/sigutcuts

# JWT Authentication
JWT_SECRET=change_me_in_production_sigutcuts_2024
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# Backend
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_TITLE=SIG-UTCUTS Chile
